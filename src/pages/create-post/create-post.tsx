// Import the function you need from react
import { CreateForm } from "./create-form";

// Export const createpost
export const CreatePost = () => {
    return (
        <div> 
            <CreateForm />
        </div>
    );
};